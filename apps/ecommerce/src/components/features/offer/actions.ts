'use server';

export async function getOfferBySlugData(slug: string) {
    try {
        return {};
    } catch (error: any) {
        console.error('error', error);
        return null;
    }
}
