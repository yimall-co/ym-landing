'use client';

import type { SearchProps } from 'pages/search';

type Props = SearchProps

export default function SearchDesktop({ t, locale }: Props) {
    return (
        <div>
            <h1>Search Desktop</h1>
        </div>
    );
}
