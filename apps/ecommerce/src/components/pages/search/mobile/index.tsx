'use client';

import type { SearchProps } from 'pages/search';

type Props = SearchProps

export default function SearchMobile({ t, locale }: Props) {
    return (
        <div>
            <h1>Search Mobile</h1>
        </div>
    );
}
