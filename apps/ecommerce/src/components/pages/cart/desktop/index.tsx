'use client';

import type { CartProps } from 'pages/cart';

type Props = CartProps

export default function CartDesktop({ t, locale }: Props) {
    return (
        <div>
            <h1>Cart Desktop</h1>
        </div>
    );
}
