'use client';

import type { CartProps } from 'pages/cart';

type Props = CartProps

export default function CartMobile({ t, locale }: Props) {
    return (
        <div>
            <h1>Cart Mobile</h1>
        </div>
    );
}
