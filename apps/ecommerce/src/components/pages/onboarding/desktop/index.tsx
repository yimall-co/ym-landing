'use client'

import type { OnboardingProps } from 'pages/onboarding';

type Props = OnboardingProps;

export default function OnboardingDesktop({ }: Props) {
    return (
        <>
            <h1>Onboarding Desktop</h1>
        </>
    );
}
