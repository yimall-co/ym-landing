'use client';

import type { SignInProps } from 'pages/sign-in';

type Props = SignInProps

export default function SignInDesktop({ }: Props) {
    'use memo'

    return (
        <div>Sign in desktop</div>
    );
}
