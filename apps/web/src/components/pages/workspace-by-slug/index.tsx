type Props = Readonly<{
    locale: string;
    workspaceSlug: string;
}>;

export default function WorkspaceBySlugPage({ locale, workspaceSlug }: Props) {
    return (
        <>
        </>
    );
}
