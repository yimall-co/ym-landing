'use client';

import type { SignUpProps } from 'pages/sign-up';

type Props = SignUpProps

export default function SignUpDesktop({ }: Props) {
    'use memo'

    return (
        <div>Sign up desktop</div>
    );
}
