'use client';

type Props = Readonly<{
    locale: string;
}>;

export default function Dashboard({ locale }: Props) {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
}
